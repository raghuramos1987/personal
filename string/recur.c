#include <stdio.h>
#include <string.h>
#include <malloc.h>
#include <stdbool.h>
#include <math.h>
#define SIZE 4

void printCoins(int in, int n[])
{
    int i;
    int temp[4];
    for(i=0;i<SIZE;i++)
    {
        if(!(in%n[i]))
        {
            printCoins(n[i],)<++>
        }
    }
    printCoins(n)<++>
}<++>

int main()
{
    int in;
    //Change SIZE if you add/sub here
    int arr[] = {25, 10, 5, 1,};
    scanf("%d",&in);
    return 0;
}
